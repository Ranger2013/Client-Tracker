// @ts-nocheck
import{clearMsg}from"../../dom/domUtils.js";import errorLogs from"../../error-messages/errorLogs.js";import{isNumeric}from"../validationUtils.js";export default async function validateDistance({evt:r}){try{if(!r.target.value)return void clearMsg({container:`${r.target.id}-error`,hide:!0,input:r.target.id});if(!isNumeric(r.target.value,!0))return"Distance must by a number."}catch(r){throw await errorLogs("validateDistanceError","Validate distance error: ",r),r}}