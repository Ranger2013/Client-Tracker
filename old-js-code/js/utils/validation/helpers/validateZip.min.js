// @ts-nocheck
import{clearMsg}from"../../dom/domUtils.js";import errorLogs from"../../error-messages/errorLogs.js";import{isNumeric}from"../validationUtils.js";export default async function validateZip({evt:r}){try{let e=r.target.value;if(!e)return void clearMsg({container:`${r.target.id}-error`,hide:!0,input:`${r.target.id}`});if(e.length>0&&e.length<5)return"Zip code must have 5 numbers.";if(e.length>5)return"Please use the 5 digit zip code.";if(!isNumeric(e,!0))return"Only numbers are allowed in the zip code."}catch(r){throw await errorLogs("validateZipError","Validate zip error: ",r),r}}