/**
 * Scrolls the window to the top of the page smoothly
 */
export function top() {
    window.scrollTo({ top: 0, behavior: 'smooth' });
}

/**
 * Scrolls a modal to its top
 * @param {HTMLElement|string} modalId - Modal element or its ID
 */
export function modalTop(modalId) {
    // We'll add your modal scroll implementation here when you find it
    // ...implementation...
}
