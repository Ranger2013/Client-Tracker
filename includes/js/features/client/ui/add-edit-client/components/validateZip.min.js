// @ts-nocheck
import{clearMsg}from"../../../../../core/utils/dom/messages";import{isNumeric}from"../../../../../core/utils/validation/validators";export default async function validateZip({evt:e}){try{let t=e.target.value;if(!t)return void clearMsg({container:`${e.target.id}-error`,hide:!0,input:`${e.target.id}`});if(t.length>0&&t.length<5)return"Zip code must have 5 numbers.";if(t.length>5)return"Please use the 5 digit zip code.";if(!isNumeric(t,!0))return"Only numbers are allowed in the zip code."}catch(e){throw e}}