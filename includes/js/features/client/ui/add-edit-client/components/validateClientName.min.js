// @ts-nocheck
import{ucwords}from"../../../../../core/utils/string/stringUtils";export default async function validateClientName({evt:e,cID:t,primaryKey:r,manageClient:a}){try{if(""===e.target.value)return"Client Name cannot be empty.";const i=await a.getClientScheduleList();if(i?.length>0){if(i.some((a=>a.primaryKey!==r&&a.cID!==t&&a.client_name.trim().toUpperCase()===e.target.value.trim().toUpperCase())))return"Client Name already in use."}return e.target.value=ucwords(e.target.value),!1}catch(e){throw e}}