// @ts-nocheck
import{isNumeric}from"../../../utils/validation/validationUtils.js";import ManageFuelCharges from"../../../classes/ManageFuelCharges.js";import{myError,mySuccess}from"../../../utils/dom/domUtils.js";export default async function handlePerMileFormSubmission(e){e.preventDefault();try{const r=document.getElementById("form-msg"),s=Object.fromEntries(new FormData(e.target));if(!isNumeric(s.starting_mile)||!isNumeric(s.cost_per_mile))return void myError(r,"All values must be numeric.");return(new ManageFuelCharges).addFuelChargesByMile(s)?void mySuccess(r,"Fuel Charges have been added."):void myError(r,"Unable to add fuel charges.<br>Please submit a new Help Desk Ticket for this issue.")}catch(e){console.warn("Handle per mile form submission error: ",e)}}