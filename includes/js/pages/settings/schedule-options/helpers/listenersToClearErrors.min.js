// @ts-nocheck
import{clearMsg}from"../../../../utils/dom/domUtils.js";import{addListener}from"../../../../utils/event-listeners/listeners.js";import{underscoreToHyphenPlusError}from"../../../../utils/string/stringUtils.js";export default function listenersToClearErrors(r){try{const e=r.querySelectorAll("input");for(const r of e)addListener(r,"focus",(e=>{clearMsg({container:underscoreToHyphenPlusError(r.id),input:r})}))}catch(r){console.warn("listeners to clear errors error: ",r)}}